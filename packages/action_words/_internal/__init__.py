"""SUT overlay of kit internals.

Business words import ``packages.action_words._internal.*``. Framework helpers
live in ``tuner_testkit.action_words._internal``; ``params.py`` stays here as
the project-data overlay.
"""
