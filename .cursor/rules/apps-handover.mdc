---
description: When an apps/ or tuner_testkit.apps tool is created or changed, it must ship a handover doc (requirement background, trial scenarios, run method, run example) and be registered in INDEX.md
globs: apps/**,tuner_testkit/apps/**
alwaysApply: false
version: 1.1.0
---

# apps — 完成期交接纪律

工具做好（或有行为变化）后，**必须**产出/更新交接文档，方便交接与试用。完整规范见
`docs/spec/apps-authoring-syntax.md` 第 4 节。

## 交接文档必须包含

写在 `apps/<name>/README.md` 或 `tuner_testkit/apps/<name>/README.md`（单文件工具写进对应目录的 README 条目）：

- **需求背景**：当时为解决什么问题而造。
- **试用场景**：适用/不适用场景与前置条件（数据源、是否需登录、依赖 extra 等）。
- **运行方式**：完整命令，含依赖安装（如 `uv sync --extra <extra>`）。
- **运行示例**：一条可复制命令 + 预期产出位置。

## 索引登记

- 在根 `INDEX.md` 第 3 节登记该工具一行（工具 / 运行 / 用途 / 交接文档链接）。
- 删除/重命名工具时同步更新 `INDEX.md` 与相关引用。

## 检查

- 交接文档的示例命令可复制即用（不含真实凭据；凭据用环境变量/占位说明）。
