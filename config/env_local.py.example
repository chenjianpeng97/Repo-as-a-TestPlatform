# 复制为 config/env_local.py（该文件已被 gitignore，勿提交）后填入本机真实值。
# env_local 只放密钥与真实 host；项目多出来的数据源别名请提交到 config/env_overlay.py。
# type 可写 mysql / sqlserver / postgres；不写则沿用 env.py / env_overlay 的 type。

DATABASES = {
    "main": {
        "host": "127.0.0.1",
        "port": 3306,
        "user": "root",
        "password": "CHANGE_ME",
        "database": "example",
    },
    # PostgreSQL 示例（Plane 本地：宿主机 127.0.0.1:5432，不要用容器名 plane-db）：
    # "main": {
    #     "type": "postgres",
    #     "host": "127.0.0.1",
    #     "port": 5432,
    #     "user": "plane",
    #     "password": "CHANGE_ME",
    #     "database": "plane",
    # },
    # 若 overlay 声明了额外别名（如 sqlserver / postgres），在此覆盖其连接信息：
    # "sqlserver": {
    #     "host": "127.0.0.1",
    #     "port": 1433,
    #     "user": "sa",
    #     "password": "CHANGE_ME",
    #     "database": "example",
    # },
}

TEST_BASE_URL = "http://127.0.0.1:8080"

TEST_ACCOUNT = {
    "username": "admin",
    "password": "CHANGE_ME",
}
