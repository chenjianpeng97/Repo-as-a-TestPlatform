# 复制为 config/env_local.py（该文件已被 gitignore，勿提交）后填入本机真实值。
# env_local 只放密钥与真实 host；项目多出来的数据源别名请提交到 config/env_overlay.py。

DATABASES = {
    "main": {
        "host": "127.0.0.1",
        "port": 3306,
        "user": "root",
        "password": "CHANGE_ME",
        "database": "example",
    },
    # 若 overlay 声明了额外别名（如 cdcs），在此覆盖其连接信息：
    # "cdcs": {
    #     "host": "127.0.0.1",
    #     "port": 1433,
    #     "user": "sa",
    #     "password": "CHANGE_ME",
    #     "database": "cdcs",
    # },
}

TEST_BASE_URL = "http://127.0.0.1:8080"

TEST_ACCOUNT = {
    "username": "admin",
    "password": "CHANGE_ME",
}
